<odoo>
    <data>
        <!-- <record id="account_move_type_view_tree" model="ir.ui.view">
            <field name="name">account.move.type.view.list</field>
            <field name="model">account.move.type</field>
            <field name="arch" type="xml">
                <list string="Types de Factures">
                    <field name="code" force_save="True" readonly="True"/>
                    <field name="name" force_save="True" readonly="type == 'automatic'"/>
                    <field name="account_id" force_save="True" readonly="True" options="{'no_create': True, 'no_open': True}"/>
                    <field name="type" force_save="True" column_invisible="True"/>
                    <field name="client_supplier" force_save="True" readonly="type == 'automatic'"/>
                    <field name="is_active" widget="boolean_toggle" force_save="True" readonly="type == 'automatic'"/>
                </list>
            </field>
        </record> -->

        <record id="account_move_type_action" model="ir.actions.act_window">
            <field name="name">Types de Factures</field>
            <field name="res_model">account.move.type</field>
            <field name="view_mode">list,form</field>
        </record>

        <menuitem
            id="account_move_type_menu"
            name="Types de Factures"
            action="account_move_type_action"
            parent="account.account_invoicing_menu"
            sequence="20"/>
    </data>
</odoo>