<odoo>
    <record id="view_affaire_year_wizard_form" model="ir.ui.view">
        <field name="name">affaire.year.wizard.form</field>
        <field name="model">dashboard.stock.wizard</field>
        <field name="arch" type="xml">
            <form class="o_form_dashboard_stock_wizard">
                <group>
                    <group>
                        <field name="site_ids" invisible="1"/>
                        <field name="site_id" options="{'no_create': True, 'no_open': True}"/>
                    </group>
                    <group>
                        <field name="type" widget="radio" options="{'horizontal': True}"/>
                    </group>
                </group>
                <footer>
                    <button name="action_confirm" type="object" string="Confirmer" class="btn-primary"/>
                    <button special="cancel" string="Annuler" class="btn-secondary"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_stock_dashboard_wizard" model="ir.actions.act_window">
        <field name="name">Tableau de bord</field>
        <field name="res_model">dashboard.stock.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>