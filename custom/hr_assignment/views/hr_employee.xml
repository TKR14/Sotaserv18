<odoo>
    <record id="view_employee_form_inherit" model="ir.ui.view">
        <field name="name">hr.employee.form.inherit</field>
        <field name="model">hr.employee</field>
        <field name="inherit_id" ref="hr.view_employee_form"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='hr_settings']" position="after">
                <page name="affectations" string="Affectations">
                    <field name="assignment_ids" options="{'no_open': True}" readonly="1">
                        <list string="Affectations" create="0" edit="0" delete="0">
                            <field name="site_id"/>
                            <field name="job_id"/>
                            <field name="volume_clocked"/>
                            <field name="uom_id"/>
                            <field name="date_start"/>
                            <field name="date_end"/>
                            <field
                                name="state"
                                widget="badge"
                                decoration-warning="state == 'planned'"
                                decoration-success="state == 'open'"
                                decoration-muted="state in ['done', 'canceled']"
                            />                            
                        </list>
                    </field>
                </page>

                <page name="pointages" string="Pointages">
                    <field name="timeclock_ids" options="{'no_open': True}" readonly="1">
                        <list string="Pointages" create="0" edit="0" delete="0">
                            <field name="date"/>
                            <field name="site_id"/>
                            <field name="job_category_id"/>
                            <field name="job_id"/>
                            <field name="is_present"/>
                            <field name="hours"/>                        
                        </list>
                    </field>
                </page>
            </xpath>             
        </field>
    </record>
</odoo>